# 🎯 Как запустить проект

## ⚡ Быстрый способ (рекомендуется)

### На macOS/Linux:
```bash
./start-all.sh
```

### На Windows:
Дважды кликните файл `start-all.bat`

Скрипт автоматически установит все зависимости и запустит приложение!

---

## 🔧 Ручной способ

### 1️⃣ Первоначальная настройка (один раз)

```bash
# Установка frontend
npm install

# Установка backend
cd server
npm install

# Создание базы данных
npm run migrate
cd ..
```

### 2️⃣ Запуск приложения (каждый раз)

Откройте **два терминала**:

**Терминал 1 - Сервер:**
```bash
cd server
npm run dev
```
Сервер запустится на http://localhost:3001

**Терминал 2 - Frontend:**
```bash
npm run dev
```
Приложение откроется на http://localhost:5173

---

## 📚 Документация

- **QUICK_START.md** - супер-быстрый старт
- **START.md** - подробные инструкции
- **DATABASE_SETUP.md** - всё о базе данных
- **INSTALLATION.md** - полная инструкция по настройке
- **README.md** - описание проекта

---

## ❓ Частые вопросы

### База данных не создаётся?
```bash
cd server
rm -f database.sqlite
npm run migrate
```

### Порт занят?
Измените порт в `server/.env`:
```
PORT=3002
```

### Ошибка подключения к API?
Убедитесь, что сервер запущен (`cd server && npm run dev`)

---

## 🎉 Готово!

После запуска откройте **http://localhost:5173** и начните работу!

Следуйте инструкциям на дашборде для первоначальной настройки:
1. Настройки → укажите константы расчёта
2. Принтеры → добавьте ваши 3D-принтеры
3. Упаковка → создайте справочник упаковки
4. Модели → создайте первую модель!
