# ⚡ Супер-быстрый запуск

## Автоматический запуск (рекомендуется)

### macOS / Linux

```bash
./start-all.sh
```

Этот скрипт автоматически:
- ✅ Установит все зависимости (если нужно)
- ✅ Создаст базу данных (если нужно)
- ✅ Запустит сервер и frontend одновременно

### Windows

Дважды кликните на файл `start-all.bat` или запустите в командной строке:

```cmd
start-all.bat
```

---

## Ручной запуск

Если автоматический скрипт не работает:

### Первый раз (настройка)

```bash
# 1. Установка frontend
npm install

# 2. Установка сервера
cd server
npm install
npm run migrate
cd ..
```

### Каждый запуск

**Терминал 1 (Сервер):**
```bash
cd server
npm run dev
```

**Терминал 2 (Frontend):**
```bash
npm run dev
```

---

## Что дальше?

После запуска откройте в браузере: **http://localhost:5173**

Следуйте инструкциям на дашборде для настройки системы.

---

## Проблемы?

### Порт занят

Если порт 3001 или 5173 уже используется:

```bash
# Измените порт сервера
# Отредактируйте server/.env:
PORT=3002

# Измените порт frontend
# Отредактируйте vite.config.ts и добавьте:
server: { port: 5174 }
```

### База данных не создаётся

```bash
cd server
rm database.sqlite
npm run migrate
```

### Сервер не отвечает

Убедитесь, что сервер запущен и проверьте логи в терминале.

---

## 🎉 Готово!

Приложение готово к работе. Удачи!
