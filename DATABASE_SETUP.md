# 🗄️ Настройка базы данных

Теперь приложение использует **реальную базу данных** (SQLite) вместо localStorage!

Все ваши данные (модели, принтеры, упаковка, настройки) теперь хранятся в базе данных на сервере.

---

## 🚀 Быстрый запуск с базой данных

### 1. Установите зависимости сервера

```bash
cd server
npm install
```

### 2. Создайте базу данных

```bash
npm run migrate
```

Эта команда создаст файл `database.sqlite` со всеми необходимыми таблицами.

### 3. Запустите сервер

```bash
npm run dev
```

Сервер запустится на порту **3001**: http://localhost:3001

### 4. Запустите frontend (в новом терминале)

```bash
cd ..
npm run dev
```

Frontend запустится на порту **5173**: http://localhost:5173

---

## 📁 Структура базы данных

База данных SQLite создаётся автоматически и содержит следующие таблицы:

### `settings` - Глобальные настройки
- Стоимость электричества
- Проценты брака и расходников
- Комиссии маркетплейсов
- API-ключ и промпты для kie.ai

### `printers` - Справочник принтеров
- Название
- Мощность потребления (кВт)

### `packaging` - Справочник упаковки
- Название
- Размеры (Д×Ш×В)
- Вес и стоимость

### `models` - Модели 3D-печати
- Основная информация (артикул, название, описание)
- Производственные данные (вес, время печати, материалы)
- Данные для WB и Ozon
- Финансовые показатели

### `model_images` - Изображения моделей
- Связаны с моделями через внешний ключ
- Хранятся как URL или base64

---

## 🔧 Конфигурация

### Переменные окружения сервера

Создайте файл `server/.env` (уже создан по умолчанию):

```env
PORT=3001
DB_PATH=./database.sqlite
NODE_ENV=development
```

### Переменные окружения frontend

Создайте файл `.env` в корне проекта:

```env
VITE_API_URL=http://localhost:3001/api
```

---

## 🔄 Миграция данных из localStorage

Если у вас уже есть данные в старой версии (localStorage):

1. Откройте старую версию приложения
2. Перейдите в **Настройки** → **Экспортировать данные**
3. Сохраните JSON-файл
4. Запустите новую версию с базой данных
5. Перейдите в **Настройки** → **Импортировать данные**
6. Загрузите сохранённый JSON-файл

---

## 📡 API Endpoints

Сервер предоставляет REST API:

### Settings
- `GET /api/settings` - Получить настройки
- `PUT /api/settings` - Обновить настройки

### Printers
- `GET /api/printers` - Список принтеров
- `GET /api/printers/:id` - Получить принтер
- `POST /api/printers` - Создать принтер
- `PUT /api/printers/:id` - Обновить принтер
- `DELETE /api/printers/:id` - Удалить принтер

### Packaging
- `GET /api/packaging` - Список упаковки
- `GET /api/packaging/:id` - Получить упаковку
- `POST /api/packaging` - Создать упаковку
- `PUT /api/packaging/:id` - Обновить упаковку
- `DELETE /api/packaging/:id` - Удалить упаковку

### Models
- `GET /api/models` - Список моделей
- `GET /api/models/:id` - Получить модель
- `POST /api/models` - Создать модель
- `PUT /api/models/:id` - Обновить модель
- `DELETE /api/models/:id` - Удалить модель

---

## 🛠️ Управление базой данных

### Просмотр базы данных

Используйте любой SQLite-клиент:
- **DB Browser for SQLite** (рекомендуется) - https://sqlitebrowser.org/
- **TablePlus** - https://tableplus.com/
- Командная строка: `sqlite3 server/database.sqlite`

### Резервное копирование

Сервер автоматически создаёт файл `database.sqlite`. Для резервной копии просто скопируйте этот файл:

```bash
cp server/database.sqlite server/database.backup.sqlite
```

Или используйте встроенный экспорт через интерфейс приложения.

### Сброс базы данных

Если нужно начать с чистого листа:

```bash
rm server/database.sqlite
cd server
npm run migrate
```

---

## 🚢 Развёртывание в продакшн

### Подготовка

1. Соберите frontend:
```bash
npm run build
```

2. Настройте переменные окружения для продакшена

### Рекомендации

- Используйте **PostgreSQL** вместо SQLite для продакшена
- Настройте **nginx** как reverse proxy
- Используйте **PM2** для управления процессом сервера
- Настройте регулярные бэкапы базы данных

---

## ❓ Решение проблем

### Сервер не запускается

```bash
# Проверьте порт
lsof -i :3001

# Измените порт в server/.env
PORT=3002
```

### База данных заблокирована

```bash
# Закройте все соединения и перезапустите сервер
rm server/database.sqlite-journal
```

### Ошибка подключения к API

Убедитесь, что:
1. Сервер запущен (`cd server && npm run dev`)
2. Frontend использует правильный URL API (проверьте `.env`)
3. Нет CORS ошибок (сервер уже настроен на разрешение CORS)

---

## 📊 Преимущества базы данных

✅ **Постоянное хранение** - данные не пропадут при очистке кэша браузера
✅ **Многопользовательский доступ** - можно работать с нескольких устройств
✅ **Надёжность** - транзакции и внешние ключи
✅ **Масштабируемость** - легко мигрировать на PostgreSQL
✅ **Резервное копирование** - простое копирование файла БД
✅ **Производительность** - индексы и оптимизированные запросы

---

**База данных готова к работе!** 🎉
