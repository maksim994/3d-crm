# Развертывание проекта 3D-печати CRM в Coolify

## Предварительные требования

- ✅ Установленный и настроенный Coolify на вашем сервере
- ✅ Git репозиторий с вашим кодом (GitHub, GitLab, Gitea и т.д.)
- ✅ Доступ к панели управления Coolify

---

## Этап 1: Подготовка проекта

### 1.1. Инициализируйте Git репозиторий (если еще не сделано)

```bash
git init
git add .
git commit -m "Initial commit - 3D Printing CRM"
```

### 1.2. Создайте репозиторий на GitHub/GitLab

```bash
# Для GitHub
git remote add origin https://github.com/ваш-username/3d-crm.git
git branch -M main
git push -u origin main
```

### 1.3. Обновите код сервера для production

Убедитесь, что файл `server/src/index.js` настроен для работы в production:

**Важные моменты:**
- Сервер должен отдавать статические файлы из папки `public`
- Порт должен читаться из переменной окружения `PORT`
- Путь к базе данных должен поддерживать переменную `DATABASE_PATH`

---

## Этап 2: Настройка в Coolify

### 2.1. Создание нового приложения

1. Откройте панель Coolify
2. Нажмите **"New Resource"** → **"Application"**
3. Выберите **"Docker Image"** или **"Public Repository"**

### 2.2. Подключение репозитория

**Если используете Git:**
- **Source**: Выберите ваш Git провайдер (GitHub/GitLab)
- **Repository**: Укажите URL вашего репозитория
- **Branch**: `main` или `master`
- **Build Pack**: `Dockerfile`

**Если используете готовый образ:**
- Сначала соберите образ локально и загрузите в Docker Hub/GHCR

### 2.3. Настройка сборки

В разделе **Build Settings**:

- ✅ **Dockerfile Location**: `/Dockerfile` (в корне проекта)
- ✅ **Build Context**: `/`
- ✅ **Port**: `3000`

### 2.4. Настройка переменных окружения

В разделе **Environment Variables** добавьте:

```env
NODE_ENV=production
PORT=3000
DATABASE_PATH=/app/data/database.sqlite
```

*(опционально, если требуется настройка CORS или других параметров)*

```env
CORS_ORIGIN=https://ваш-домен.com
API_BASE_URL=https://ваш-домен.com
```

### 2.5. Настройка Persistent Storage (ВАЖНО!)

**Это критично для сохранения базы данных между перезапусками!**

1. Перейдите в раздел **"Storages"** или **"Volumes"**
2. Нажмите **"Add Persistent Storage"**
3. Настройте volume:
   - **Name**: `3d-crm-database`
   - **Mount Path**: `/app/data`
   - **Source Path**: `/data/3d-crm-database` (или любой путь на сервере)

Это обеспечит, что файл `database.sqlite` будет сохраняться между перезапусками контейнера.

---

## Этап 3: Развертывание

### 3.1. Запуск деплоя

1. Нажмите кнопку **"Deploy"**
2. Coolify автоматически:
   - Клонирует репозиторий
   - Соберет Docker образ
   - Запустит контейнер
   - Выполнит миграции базы данных

### 3.2. Мониторинг процесса

Следите за логами деплоя в реальном времени:
- Проверьте, что сборка фронтенда прошла успешно
- Убедитесь, что миграции базы данных выполнились
- Проверьте, что сервер запустился на порту 3000

### 3.3. Настройка домена

В разделе **"Domains"**:

1. Добавьте ваш домен: `3d-crm.yourdomain.com`
2. Coolify автоматически настроит SSL через Let's Encrypt
3. Подождите 1-2 минуты для активации сертификата

---

## Этап 4: Проверка работы

### 4.1. Проверьте доступность

Откройте ваш домен в браузере:
```
https://3d-crm.yourdomain.com
```

### 4.2. Проверьте API

```bash
curl https://3d-crm.yourdomain.com/api/health
```

### 4.3. Проверьте логи

В Coolify откройте вкладку **"Logs"** и убедитесь, что нет ошибок:
```
Server running on port 3000
Database connected successfully
```

---

## Этап 5: Резервное копирование

### 5.1. Настройка автоматического бэкапа

В Coolify можно настроить автоматическое резервное копирование volume с базой данных:

1. Перейдите в **"Backups"**
2. Настройте расписание (например, ежедневно в 3:00)
3. Укажите место хранения (S3, локальное хранилище)

### 5.2. Ручной бэкап

**Через веб-интерфейс:**
- Зайдите в приложение → Настройки → Экспорт данных

**Через SSH на сервере:**
```bash
# Найдите путь к volume
docker volume inspect 3d-crm-database

# Скопируйте базу данных
docker cp <container-id>:/app/data/database.sqlite ./backup-$(date +%Y%m%d).sqlite
```

---

## Этап 6: Обновление приложения

### 6.1. Автоматические обновления

Настройте webhook для автоматического деплоя:

1. В Coolify скопируйте Webhook URL
2. Добавьте его в настройки репозитория (GitHub → Settings → Webhooks)
3. Теперь каждый push в `main` будет автоматически деплоить новую версию

### 6.2. Ручное обновление

```bash
# На локальной машине
git add .
git commit -m "Update: описание изменений"
git push origin main

# В Coolify нажмите "Redeploy"
```

---

## Этап 7: Оптимизация (опционально)

### 7.1. Health checks

В настройках приложения добавьте health check:
- **Path**: `/api/health`
- **Interval**: 30 секунд
- **Timeout**: 5 секунд

### 7.2. Resource limits

Установите лимиты ресурсов:
- **Memory**: 512MB - 1GB (для небольшого проекта)
- **CPU**: 0.5 - 1 CPU

### 7.3. Мониторинг

Настройте уведомления в Coolify:
- Email при падении приложения
- Slack/Telegram webhook для алертов

---

## Возможные проблемы и решения

### Проблема 1: База данных не сохраняется
**Решение**: Убедитесь, что persistent storage правильно настроен и примонтирован к `/app/data`

### Проблема 2: Ошибка при сборке better-sqlite3
**Решение**: В Dockerfile уже добавлены необходимые зависимости (python3, make, g++)

### Проблема 3: Frontend не загружается
**Решение**: Проверьте, что сервер правильно настроен для отдачи статических файлов из папки `public`

### Проблема 4: CORS ошибки
**Решение**: Добавьте переменную окружения `CORS_ORIGIN` с вашим доменом

---

## Дополнительные рекомендации

### Безопасность

1. ✅ Используйте HTTPS (Coolify настраивает автоматически)
2. ✅ Настройте регулярные бэкапы базы данных
3. ✅ Ограничьте доступ к панели управления Coolify
4. ✅ Используйте сильные пароли для SSH

### Производительность

1. ✅ Настройте CDN для статических файлов (опционально)
2. ✅ Включите gzip сжатие в Express
3. ✅ Используйте кеширование для API запросов

### Масштабирование

Если проект вырастет:
1. Мигрируйте с SQLite на PostgreSQL
2. Разделите frontend и backend на отдельные сервисы
3. Используйте Redis для кеширования
4. Настройте load balancer

---

## Полезные команды

```bash
# Просмотр логов контейнера
docker logs -f <container-name>

# Подключение к контейнеру
docker exec -it <container-name> sh

# Проверка volume
docker volume ls
docker volume inspect 3d-crm-database

# Бэкап базы данных
docker cp <container-id>:/app/data/database.sqlite ./backup.sqlite

# Восстановление базы данных
docker cp ./backup.sqlite <container-id>:/app/data/database.sqlite
```

---

## Контакты и поддержка

Если возникнут вопросы:
- 📖 [Документация Coolify](https://coolify.io/docs)
- 💬 [Discord Coolify](https://discord.gg/coolify)
- 🐛 [GitHub Issues](https://github.com/coollabsio/coolify)

---

**Готово! После выполнения всех этапов ваше приложение будет доступно в интернете с автоматическими обновлениями и резервным копированием. 🚀**
