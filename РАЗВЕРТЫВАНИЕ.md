# 🚀 Развертывание в Coolify - Краткое руководство

## Что готово

Ваш проект **полностью подготовлен** к развертыванию в Coolify! Все необходимые файлы созданы.

---

## 📋 Пошаговая инструкция

### Шаг 1: Создать Git репозиторий (2 минуты)

```bash
# Если еще не создан git репозиторий
git init
git add .
git commit -m "feat: готов к деплою в Coolify"

# Создайте репозиторий на GitHub/GitLab, затем:
git remote add origin https://github.com/ваш-username/3d-crm.git
git branch -M main
git push -u origin main
```

### Шаг 2: Настроить в Coolify (3 минуты)

1. Откройте панель Coolify → **New Resource** → **Application**

2. **Подключите репозиторий:**
   - Source: GitHub/GitLab
   - Repository: URL вашего репозитория
   - Branch: `main`

3. **Build Settings:**
   - Build Pack: `Dockerfile`
   - Port: `3000`

4. **Environment Variables:**
   ```env
   NODE_ENV=production
   PORT=3000
   DATABASE_PATH=/app/data/database.sqlite
   ```

5. **⚠️ ВАЖНО! Persistent Storage:**
   - Добавить Volume
   - Mount Path: `/app/data`
   - Name: `3d-crm-database`
   
   *Без этого база данных будет теряться при перезапуске!*

6. **Домен:**
   - Добавьте ваш домен (например: `crm.yourdomain.com`)
   - SSL сертификат настроится автоматически

### Шаг 3: Деплой (5-10 минут)

1. Нажмите кнопку **Deploy**
2. Следите за логами сборки
3. Дождитесь сообщения: "Server running on port 3000"

### Шаг 4: Проверка (1 минута)

```bash
# Проверьте API
curl https://ваш-домен.com/api/health

# Откройте в браузере
https://ваш-домен.com
```

---

## 📚 Детальные инструкции

У вас есть 3 документа на выбор:

1. **`COOLIFY_CHECKLIST.md`** ✅
   - Быстрый чек-лист на 5-10 минут
   - Минимум текста, максимум действий
   - **Рекомендуется для быстрого старта**

2. **`COOLIFY_DEPLOYMENT.md`** 📖
   - Полное руководство на 7 этапов
   - Со всеми деталями и объяснениями
   - Решение проблем и оптимизация
   - **Рекомендуется прочитать после первого деплоя**

3. **`DEPLOYMENT_OVERVIEW.md`** 🗺️
   - Обзор архитектуры и процесса
   - Диаграммы и схемы
   - Команды для работы
   - **Для понимания как все работает**

---

## ⚡ Что изменилось в коде

### 1. Создан `Dockerfile`
Multi-stage сборка, которая:
- Собирает React фронтенд
- Устанавливает Node.js бэкенд
- Объединяет все в один контейнер

### 2. Обновлен `server/src/index.js`
- Добавлена поддержка production режима
- Сервер теперь отдает статические файлы из `/public`
- Настроен роутинг для React SPA
- Поддержка CORS через переменные окружения

### 3. Обновлен `server/src/db/database.js`
- Поддержка переменной `DATABASE_PATH`
- Логирование пути к базе данных

Все изменения обратно совместимы - локальная разработка работает как прежде!

---

## 🔄 Автоматический деплой (опционально)

### После первого успешного деплоя:

1. В Coolify скопируйте **Webhook URL**
2. В GitHub/GitLab:
   - Settings → Webhooks → Add webhook
   - Вставьте URL из Coolify
3. Теперь каждый `git push` автоматически деплоит новую версию!

---

## 💾 Резервное копирование

### Способ 1: Через веб-интерфейс
Настройки → Экспорт данных → Скачать JSON

### Способ 2: Через Docker
```bash
# На сервере Coolify
docker cp <container-id>:/app/data/database.sqlite ./backup.sqlite
```

### Способ 3: Автоматический
В Coolify настройте автоматический бэкап volume (ежедневно в 3:00 ночи)

---

## 🛠️ Частые вопросы

### Данные теряются при перезапуске?
✅ Убедитесь, что настроен **Persistent Storage** с mount path `/app/data`

### Не загружается фронтенд?
✅ Проверьте, что `NODE_ENV=production` в переменных окружения

### CORS ошибки?
✅ Добавьте переменную `CORS_ORIGIN=https://ваш-домен.com`

### Ошибка при сборке?
✅ Проверьте логи - возможно не хватает зависимостей (уже добавлены в Dockerfile)

---

## 📊 Рекомендуемые ресурсы сервера

Для вашего приложения достаточно:
- **CPU**: 0.5-1 core
- **RAM**: 512MB - 1GB
- **Disk**: 5-10GB

Подходящие VPS:
- Hetzner CX11 (2.89€/мес)
- DigitalOcean Basic ($6/мес)
- Contabo VPS S ($3.99/мес)

---

## 🎯 Что дальше?

### После успешного деплоя:

1. ✅ Настроить автоматический деплой (webhook)
2. ✅ Настроить автоматическое резервное копирование
3. ✅ Настроить мониторинг и алерты
4. ✅ Добавить Health Check в Coolify
5. ✅ Протестировать работу приложения

### Для обновления приложения:

```bash
# 1. Внести изменения в код
# 2. Закоммитить и запушить
git add .
git commit -m "feat: новая функция"
git push origin main

# 3. Если настроен webhook - деплой произойдет автоматически!
```

---

## 📞 Поддержка

Если что-то не получается:

1. 📖 Проверьте **COOLIFY_DEPLOYMENT.md** - там есть раздел "Возможные проблемы"
2. 🌐 [Документация Coolify](https://coolify.io/docs)
3. 💬 [Discord Coolify](https://discord.gg/coolify)
4. 📝 Проверьте логи в Coolify (вкладка "Logs")

---

## ✅ Финальный чек-лист

- [ ] Git репозиторий создан и код запушен
- [ ] Приложение создано в Coolify
- [ ] Dockerfile указан как Build Pack
- [ ] Порт установлен в 3000
- [ ] Переменные окружения настроены
- [ ] **Persistent Storage настроен** (критично!)
- [ ] Домен добавлен и SSL активирован
- [ ] Деплой запущен и прошел успешно
- [ ] Приложение открывается в браузере
- [ ] API работает (`/api/health`)
- [ ] Данные сохраняются после перезапуска

---

**🎉 Успехов с развертыванием! Если следовать инструкциям, все займет 10-15 минут.**

**Начните с `COOLIFY_CHECKLIST.md` для быстрого старта!**
