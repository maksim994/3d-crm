# 🎉 База данных добавлена успешно!

## ✅ Что изменилось

Теперь ваше приложение использует **настоящую базу данных SQLite** вместо localStorage!

### Преимущества:

✅ **Постоянное хранение** - данные не пропадут при очистке браузера
✅ **Надёжность** - транзакции и внешние ключи
✅ **Масштабируемость** - легко мигрировать на PostgreSQL
✅ **Многопользовательский доступ** - можно работать с нескольких устройств
✅ **Простые бэкапы** - просто скопируйте файл базы данных

---

## 🚀 Как запустить

### Автоматический запуск (самый простой способ)

#### macOS/Linux:
```bash
./start-all.sh
```

#### Windows:
Дважды кликните `start-all.bat`

Скрипт сам установит зависимости, создаст базу данных и запустит всё!

---

### Ручной запуск

Если автоматический скрипт не работает:

**1. Установка (первый раз):**
```bash
# Frontend
npm install

# Backend
cd server
npm install
npm run migrate
```

**2. Запуск (каждый раз):**

Откройте 2 терминала:

**Терминал 1:**
```bash
cd server
npm run dev
```

**Терминал 2:**
```bash
npm run dev
```

Откройте браузер: **http://localhost:5173**

---

## 📁 Структура проекта

```
3d/
├── server/              # 🆕 Backend сервер
│   ├── src/
│   │   ├── db/         # База данных и миграции
│   │   ├── routes/     # REST API endpoints
│   │   └── index.js    # Сервер Express
│   ├── .env            # Настройки сервера
│   └── database.sqlite # 🗄️ База данных (создаётся автоматически)
│
├── src/
│   ├── api/            # 🆕 API клиент для общения с сервером
│   ├── store/          # ✏️ Обновлён для работы с API
│   ├── pages/          # Страницы приложения
│   └── components/     # UI компоненты
│
└── [документация]
    ├── HOW_TO_START.md         # Как запустить (читайте первым!)
    ├── QUICK_START.md          # Быстрый старт
    ├── DATABASE_SETUP.md       # Подробно о базе данных
    └── README.md               # Описание проекта
```

---

## 🗄️ База данных

### Где хранятся данные?

Файл: `server/database.sqlite`

### Таблицы:

- **settings** - Глобальные настройки
- **printers** - Справочник принтеров
- **packaging** - Справочник упаковки
- **models** - Модели 3D-печати
- **model_images** - Изображения моделей

### Резервное копирование:

**Способ 1 (через интерфейс):**
Настройки → Экспортировать данные

**Способ 2 (файл БД):**
```bash
cp server/database.sqlite server/database.backup.sqlite
```

---

## 🔄 Миграция старых данных

Если у вас были данные в старой версии (localStorage):

1. Откройте старую версию
2. Настройки → Экспорт данных → сохраните JSON
3. Запустите новую версию
4. Настройки → Импорт данных → загрузите JSON

---

## 🛠️ API Endpoints

Сервер предоставляет REST API на порту 3001:

```
GET    /api/settings          - Настройки
PUT    /api/settings          - Обновить настройки

GET    /api/printers          - Список принтеров
POST   /api/printers          - Создать принтер
PUT    /api/printers/:id      - Обновить
DELETE /api/printers/:id      - Удалить

GET    /api/packaging         - Список упаковки
POST   /api/packaging         - Создать
PUT    /api/packaging/:id     - Обновить
DELETE /api/packaging/:id     - Удалить

GET    /api/models            - Список моделей
POST   /api/models            - Создать модель
PUT    /api/models/:id        - Обновить
DELETE /api/models/:id        - Удалить

GET    /api/health            - Проверка работы сервера
```

---

## ⚙️ Настройки

### Порты

**Сервер:** 3001 (измените в `server/.env`)
**Frontend:** 5173 (по умолчанию Vite)

### Переменные окружения

**server/.env:**
```env
PORT=3001
DB_PATH=./database.sqlite
NODE_ENV=development
```

**.env (опционально):**
```env
VITE_API_URL=http://localhost:3001/api
```

---

## ❓ Решение проблем

### Сервер не запускается

```bash
# Проверьте порт
lsof -i :3001

# Измените порт
# Отредактируйте server/.env: PORT=3002
```

### Ошибка подключения к API

Убедитесь, что:
1. ✅ Сервер запущен (`cd server && npm run dev`)
2. ✅ Порт 3001 не занят
3. ✅ В консоли браузера нет CORS ошибок

### База данных не создаётся

```bash
cd server
rm -f database.sqlite
npm run migrate
```

### "Cannot find module"

```bash
# Переустановите зависимости
rm -rf node_modules server/node_modules
npm install
cd server && npm install
```

---

## 📚 Дополнительные материалы

- **HOW_TO_START.md** - Как запустить (начните отсюда!)
- **QUICK_START.md** - Супер-быстрый старт
- **DATABASE_SETUP.md** - Всё о базе данных
- **START.md** - Подробные инструкции
- **INSTALLATION.md** - Полная инструкция
- **server/README.md** - Документация по backend

---

## 🎯 Что дальше?

1. Запустите приложение (см. выше)
2. Откройте http://localhost:5173
3. Следуйте инструкциям на дашборде:
   - Настройки → укажите константы
   - Принтеры → добавьте принтеры
   - Упаковка → создайте справочник
   - Модели → создайте первую модель!

---

## 💡 Полезные команды

```bash
# Просмотр базы данных
sqlite3 server/database.sqlite

# Экспорт дампа БД
sqlite3 server/database.sqlite .dump > backup.sql

# Логи сервера
cd server && npm run dev

# Пересоздать БД
cd server && rm database.sqlite && npm run migrate
```

---

## 🎉 Готово!

Ваше приложение теперь работает с полноценной базой данных!

**Приятной работы!** 🚀
